/**
 * Date de référence « aujourd’hui » partagée (maquette alignée sur oct. 2026).
 * Pour la production, remplacer par `new Date()`.
 */
export function getAppToday(): Date {
  return new Date(2026, 9, 15)
}
